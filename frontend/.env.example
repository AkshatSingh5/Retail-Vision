# Frontend-safe variables only. Anything here is visible in the browser.

# GPU backend origin. Empty means same-origin (UI served by FastAPI at :8000).
# On Vercel this MUST be the public HTTPS origin of FastAPI, not the Vercel URL.
API_BASE_URL=http://localhost:8000
